import React from 'react';
import 'twin.macro';
import tw, { css } from 'twin.macro';
import moment from 'moment';
import Button from '../Utils/Button';

interface CustomerBookingHistoryCardProps {
    customerName: string;
    bookingDate: string;
    moneyPaid: number;
}

export const CustomerBookingHistoryCard: React.FC<CustomerBookingHistoryCardProps> = (props) => {
    return(<></>)
}